# Copy values into your shell or hosting dashboard secrets.
# Vite local bindings read process.env when starting `npm run dev`.

PERSONAL_ADMIN_PASSWORD=qq-weekend-dev
RESEND_API_KEY=
RESEND_FROM_EMAIL=QQ Weekend <onboarding@resend.dev>
